"""Routes for the dashboard app: aggregate counts and recent activity."""

from django.urls import path

# from . import views

urlpatterns = [
    # path('summary/', views.DashboardSummaryView.as_view(), name='dashboard-summary'),
]
